name: Apply recipe fixes
on:
  push:
    paths:
      - 'recipe-fixes.js'
      - '.github/workflows/apply-recipe-fixes.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Force fresh fixes script
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import re
          p=Path('index.html')
          s=p.read_text()
          s=re.sub(r'<script src="recipe-fixes\.js(?:\?v=[^"]*)?"></script>', '<script src="recipe-fixes.js?v=5"></script>', s)
          if 'recipe-fixes.js?v=5' not in s:
              s=s.replace('</body>','<script src="recipe-fixes.js?v=5"></script></body>')
          p.write_text(s)
          PY
      - name: Commit
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add index.html
          git diff --cached --quiet || git commit -m 'Przywróć stary wygląd kategorii i odśwież poprawki'
          git push