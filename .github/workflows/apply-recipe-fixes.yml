name: Apply recipe fixes
on:
  push:
    paths:
      - 'recipe-fixes.js'
      - 'advisor-pro.js'
      - 'recipes-26-50.js'
      - '.github/workflows/apply-recipe-fixes.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Connect current features
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import re
          p=Path('index.html')
          s=p.read_text()
          s=re.sub(r'<script src="recipe-fixes\.js(?:\?v=[^"]*)?"></script>', '', s)
          s=re.sub(r'<script src="advisor(?:-pro)?\.js(?:\?v=[^"]*)?"></script>', '', s)
          s=re.sub(r'<script src="recipes-26-50\.js(?:\?v=[^"]*)?"></script>', '', s)
          s=s.replace('</body>','<script src="recipes-26-50.js?v=1"></script><script src="recipe-fixes.js?v=8"></script><script src="advisor-pro.js?v=3"></script></body>')
          p.write_text(s)
          PY
      - name: Commit
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add index.html
          git diff --cached --quiet || git commit -m 'Włącz 25 nowych przepisów'
          git push